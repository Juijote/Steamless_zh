name: Build Software

on:
  push:
    branches:
      - main  # 可以根据你的主要分支名称进行修改

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Build solution
      run: |
        dotnet build Steamless.sln
